# Vercel / server: add in Project Settings → Environment Variables
# Used by api/product-insights.js (never commit real keys)
OPENAI_API_KEY=
# Optional override (default gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
